span.tags-punctuation
  display flex
  align-items center

.bber-reply
  cursor pointer
  margin 0 4px

  &:hover
    color var(--flec-main)

.post-tips
  color var(--flec-gray)
  font-size 14px;
  position absolute
  bottom 1rem
  left 2rem

  +maxWidth768()
    left 1rem

  a
    color var(--flec-gray)
    border none
    margin-left 4px

    &:hover
      color var(--flec-main)